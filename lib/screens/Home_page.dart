import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  HomePage({super.key});
  Color primarycolor = Color(0xff9AE66E);
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
